# Get username of system
whoami

# Get OS version of system
uname -r

# Get IP of system
ifconfig
ip addr

# Get Network Connections to system
netstat
ss

# Get Group of current user
id

# Find string in a file
Cat file.txt > grep "string to find"

# Clear Bash History
cat /dev/null > ~/.bash_history
history -c
