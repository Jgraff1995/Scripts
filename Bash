# Get username of system
whoami

# Get OS version of system
uname -r

# Get IP of system
ifconfig
ip addr

# Get Network Connections to system
netstat
ss

# Get Group of current user
id

# Find string in a file
Cat file.txt > grep "string to find"

# Clear Bash History
cat /dev/null > ~/.bash_history
history -c

# view hidden files
ls -la

# Use history to redo a command, (This will redo the 37th command in history)
history
!37

# Change permission on file (rwx)
chmod

# Add user
useradd

# Reverse Search
Ctrl+r
